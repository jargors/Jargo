name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: install javafx
      run: sudo apt install openjfx
    - name: make jar
      run: make jar
